version: 1
frontend:
  phases:
    preBuild:
      commands:
        - bun i
    build:
      commands:
        - bun run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
